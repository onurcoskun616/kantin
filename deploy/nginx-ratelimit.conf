# /etc/nginx/conf.d/kantin-ratelimit.conf olarak kopyalayin.
# limit_req_zone yalnizca http blogunda tanimlanabilir, bu yuzden ayri dosyadadir.
limit_req_zone $binary_remote_addr zone=kantin_login:10m rate=10r/m;
